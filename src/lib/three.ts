// Centralized Three.js import to prevent multiple instances
// All components MUST import from this file instead of directly from 'three'

import * as THREE from 'three';

// Export as default to ensure single instance across the app
export default THREE;
